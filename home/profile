# vim: filetype=sh
export CARGO_HOME=$HOME/.local/share/cargo
export RUSTUP_HOME=$HOME/.local/share/rustup
export DENO_INSTALL=$HOME/.local

for p in $CARGO_HOME $HOME/.local
do
  case ":$PATH:" in
    *:$p/bin:*)
      ;;
    *)
      PATH=$p/bin:$PATH
  esac
done
